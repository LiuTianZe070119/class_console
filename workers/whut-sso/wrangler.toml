name = "whut-sso"
main = "index.js"
compatibility_date = "2024-01-01"

# 免费计划足够班级使用（每天10万次请求）
# 如需绑定自定义域名，取消下面注释：
# routes = [
#   { pattern = "sso.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]
